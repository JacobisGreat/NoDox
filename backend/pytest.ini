[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    network: hits the live Instagram endpoint; skipped unless RUN_NETWORK_TESTS=1
filterwarnings =
    ignore::DeprecationWarning
